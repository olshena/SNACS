#' Example mutation data
#'
#' The mutation data should be a numeric matrix with SNPs in the rows and cells in the columns. 1s represent mutations and 0s non-mutations.
#'
#' @format ## `mutMat`
#' A matrix with 24 rows and 750 columns:
"mutMat"


#' Example hash antibody data
#'
#' The antibody data should be a matrix with hashes in the rows and cells in the columns. The hash antibody value should be a continuous numeric measure.
#'   ...
#'
#' @format ## `hashMat`
#' A matrix with 3 rows and 750 columns:
"hashMat"
